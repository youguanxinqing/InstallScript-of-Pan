#!/bin/bash

# 格式化输出
function print_format
{
	local symbol
	local blank
	strlen=$((`expr length "$1"` + 2))
	for((i=0;i<strlen;i++))
	do
		symbol="$symbol-"
		blank="$blank "
	done
	
	echo "$symbol--"
	echo "|$blank|"
	echo "| $1 |"
	echo "|$blank|"
	echo "$symbol--"
}

